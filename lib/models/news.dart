class News {
  String author, publishedAt, title, description, image, content;

  News(
      {required this.author,
      required this.publishedAt,
      required this.title,
      required this.description,
      required this.image,
      required this.content});
}
